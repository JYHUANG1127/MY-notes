1. list comprehension 列表表达式，快速创建一个想要的表格的方法，把一个对list操作的函数浓缩成一个包含了函数作用的list
![[Pasted image 20251228171935.png]]
它的基本形式如下：
![[Pasted image 20251228171947.png]]
2. 默认参数，意味着如果使用者没有输入这个参数就等于创建者设定的参数，也意味着如果使用者额外输入了，这个参数就变成使用者输入的值了。它的形式如下:这个在函数定义里输入的epsilon就是默认参数（ps.默认参数必须在最后面，不能在前面）
![[Pasted image 20251228172008.png]]
在使用时，如果没给函数定义中的参数名称输入，就必须按照顺序，如果有的话，就不需要按顺序：
![[Pasted image 20251228172024.png]]
3. 函数不仅可以作为参数，也可以作为返回值被返回
![[Pasted image 20251228172041.png]]
    对这段代码的解释是，首先定义了一个函数，名称为make_prod（a），这个函数的作用是【定义一个新的函数g（b），返回a_b】，然后先定义的函数返回的是g，这个函数名称，所以make_prod(2),返回的就是a=2时的函数g，然后g（b）等于a_b，所以val等于6.在g中其实是不存在a是多少的，但是在这个小环境中没有定义的参数名称如果python没找到定义，就会一层层的往大环境中找，直到找到a=2
4. 为了后续test和debug更简单，可以在开始写好document文档，说明应该输入什么，会得到什么，确保这段代码是干嘛的
5. black box testing 黑盒测试，不看代码，仅仅只看规范和文档字符串来测试，与之相对的是glass box testing，通过代码来直到编写的测试用例，这个测试用例意味着将经历程序中的每一条路径
6. 一些简单的典型错误，indexerror，索引出了错误，比如超出了索引的范围，typeerror，类型出了错误，nameerror，可能出现了一个从未初始化的额变量，syntaxerror通常意味着你的缩进不对，或者遗漏括号这种的小错误。logic error比较麻烦，通常要查多行代码
7. 一些tips for debug:找到多数测试失败案例的共同点；记录循环变量或者各种变量在循环中的值；测试过程的二分法，在代码的中间放置print语句，如果正常，问题就在代码部分的后面，如果出了问题，那么见面就有问题
8. python可以编写异常处理器来处理运行过程中出现的error，exception，它的形式如下，在try后面，告诉python运行下面的代码，如果运行中出现了异常，python会立即停止，然后运行下面的except代码，根据错误的类型，还可以更改except语句
![[Pasted image 20251228172123.png]]
![[Pasted image 20251228172134.png]]
不知except语句，可以和try合起来用的还有else，finally语句。当try中的所有代码都正常运行时，就会跳转else语句。无论发生什么，finally语句都会被运行

9. 当我们不想代码带着错误继续运行时，可以主动引进一个error，然后使得程序崩溃停止，并且带上我们输入的消息
![[Pasted image 20251228172152.png]]
10. assert语句，用于强化函数编写者于使用者之间的合同
![[Pasted image 20251228172212.png]]
这是它的具体形式，当assert不被满足后，就会引入asserterror
11. dictionary，python中的dictionary时一个复合数据类型，就像不可更改的tuples和可以更改的list一样。在创建一个dictionary或者list时，都是创建了一个映射，比如在list中就是将0，1，2，3映射到它储存的值，而dictionary不强行规定映射前是啥，它都行，被称为Key
![[Pasted image 20251228172229.png]]
12. 创建一个dictionary是用大括号，具体格式是name={'key1':'val1','key2':'val2'}（在dic中，3.6版本前是不存在顺序的，在3.6后的顺序是输入的顺序）
![[Pasted image 20251228172242.png]]
要查找key对应的值的话方法和list一样，name['key'],但是不可能根据val找到key
![[Pasted image 20251228172256.png]]
要向dictionary中添加一个数据，用下面的形式，如果python没找到前面的key 的话，就会自动添加进去，如果找到了key，就会更改key映射的值
![[Pasted image 20251228172309.png]]
del（）可以直接删去dictionary中的某个值
![[Pasted image 20251228172323.png]]
13. 要检查某个key是否存在于dictionary中，只是检查key，不检查val
![[Pasted image 20251228172339.png]]
如果要查看一个dic中所有的key
![[Pasted image 20251228172352.png]]
相同，如果要查看所有的val
![[Pasted image 20251228172407.png]]
要同时查看key和val，使用items语句，
![[Pasted image 20251228172424.png]]
如果在for loop中使用items，给出两个参数，就意味着一个是key，后一个是val
![[Pasted image 20251228172505.png]]
上述的list都是把后面的类型转换为list类型

14. key的一些限制：key必须唯一，必须immutable，也就是说不能是list和另一个dictionary ，key不可变的原因是意味python储存key用的是哈希函数，不同key得到的值不同
但是val可以是另一个dic
![[Pasted image 20251228172533.png]]
![[Pasted image 20251228172716.png]]
查看时用两个key就行了
![[Pasted image 20251228172812.png]]
15. lower.()小写函数，把大写的变成小写的