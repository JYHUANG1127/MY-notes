1. recursion 递归算法，对原始问题，使用一种稍有不同的方法解决问题，比如把5_4,看成5+5_3。直到5+5+5+51这种.把原问题一步步简化，直到找到一个基本事实，就可以直接解决.
![[Pasted image 20251228172828.png]]
在return里又带本身函数的定义，就会创建多个环境，直到满足b1的情况，然后一层层递归到第一层函数。递归算法与iteration迭代算法（前面的while loop，for loop）最大的区别就是迭代中的每次loop都会得到一个结果，虽然不一定return，但是是有的，而递归是到最后才有结果返回，然后一层层往上递归，才得到最终结果
![[Pasted image 20251228172950.png]]
2. 如果递归中有几步反复计算，可以在递归中添加dic，然后检查是否出现过，可以减少计算的步数
![[Pasted image 20251228173002.png]]
3. 在函数的return中，最好是不管任何情况都return同一个type的对象，不然可能会在后续应用中出现问题。一般recursion是用来处理嵌套列表的方法，当你有不确定长度或者深度的数据结构时，用递归就很合适
4. 当我们想要创建自己的对象类型时，有两件事需要考虑，首先是怎么把这个object 抽象数字化，就像把现实世界里的一辆车变成它的设计蓝图一样，还有就是怎么让使用者和其他的object交互
5. 以创建一个二维平面的坐标的类型class为例，其代码如下：
![[Pasted image 20251228173013.png]]
最关键的是两个属性，离x轴多远，离y轴多远（抽象数字化），就是创建一个处理这种特定类型对象的函数，也就是说这个函数只能处理这种你创建type的数据。这个object是你创建对象类型的父类型，意味着object能做的，你的type都能做，还能做你接下来设定的。
![[Pasted image 20251228173026.png]]
![[Pasted image 20251228173041.png]]
这个self的基本理念是它总是这个方法的第一个参数，为什么这里使用self是因为现在只是告诉python向创建这个对象类型，还没有实际可操作的对象。这个主题self.x和self.y包含你想用来初始化对象的任何代码。如果没有self，xy在创建这个类型的对象后就会消失，因为函数已经中止了。（ps在这个特定对象的交互函数里，函数名（init）前和后必须携程双下划线__）
![[Pasted image 20251228173055.png]]
以现实世界类比：如果我要创造一个房子（type），房子的蓝图中最重要的属性是sofa，coffee table，chair（x，y），那么self.coffee_table就意味着如果将来我有一个房子，self.coffee_table就指代这个房子里的coffee table。当真的有这个房子时，就不会用self了，直接就是room's coffee table 就行。所以self仅仅只在蓝图中使用

6. 上面时定义一个type，创建一个这个type的object如下：
![[Pasted image 20251228173107.png]]
不用加self，直接给核心数据，如果要查看这个object的某个属性，可以用点符号'.'
![[Pasted image 20251228173118.png]]
7. 通常我们还要创建一个method专门用来处理这个type的数据
![[Pasted image 20251228173132.png]]
8. 点符号'.'的使用
![[Pasted image 20251228173145.png]]
在点符号前时我向调用这个method的对象，然后'.'，然后是method，然后事使用这个method需要的参数
![[Pasted image 20251228173201.png]]
![[Pasted image 20251228173212.png]]
这两个相同是因为前者的self自动变成了object c了，然后调到前面去了，也就是说在实际运行中，第一个参数都会跑到点之前。后者是告诉python你想要处理的对象的类型是coordinate，然后需要输入全部参数

9. 在定义一个type时，也可以强制要求输入的某个参数的type，以创建一个名为circle的type为例，它的核心属性是圆心所处位置和半径，我们可以要求输入的第一个参数为坐标，也就是上面的coordinate
![[Pasted image 20251228173228.png]]
10. 当你使用+-/时其实只是用了python中的快捷表示法，当你使用这些操作时，在python中其实时调用了下面的函数
![[Pasted image 20251228173241.png]]
如果你想让你编写类型的对象和=-+*/等一系列符号一起使用，你必须在定义的时候也要编写进去

以print为例
![[Pasted image 20251228173252.png]]